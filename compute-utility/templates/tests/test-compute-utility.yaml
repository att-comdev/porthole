apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: {{ .Release.Name }}-basic-test
    image: {{ .Values.images.tags.compute_utility }}
    env:
    command: ["bash", "-c", "kubectl --help"]
  restartPolicy: Never
