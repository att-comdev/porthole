Generic Docker Makefile
-----------------------

This is a generic make and dockerfile for mysql-client utility container, which
can be used to create docker images using different releases.

Usage:

make IMAGE_TAG=<mysql_version>

eg:

1. Create docker image for mysql-client v1.0.0

  make IMAGE_TAG=v1.0.0
